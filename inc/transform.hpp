#pragma once
#include "matrix.hpp"

namespace sf {
namespace Transform {

template <class T>
Matrix<T, 4> translate(const Matrix<T, 4>& mat, const Vector<T, 4>& vec) {
  // todo
  abort();
}
template <class T>
Matrix<T, 4> rotate(const Matrix<T, 4>& mat, float angle,
                    const Vector<T, 4>& vec) {
  // todo
  abort();
}
template <class T>
Matrix<T, 4> scale(const Matrix<float, 4>& mat, const Vector<T, 4>& vec) {
  // todo
  abort();
}

}  // namespace Transform
}  // namespace sf